language: ruby

install: gem install rake

script:
  - pwd
  - cd ~ && ls
  - git clone git://github.com/sciruby-jp/rubydown && cd rubydown && sudo rake build && sudo rake install
  - ls /usr/local/bundle/bin/rubydown
  
